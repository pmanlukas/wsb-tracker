# WSB Tracker Configuration
# Copy this file to .env and customize as needed

# =============================================================================
# Reddit API Credentials (OPTIONAL)
# =============================================================================
# Note: Reddit now requires approval for API access.
# The tracker works without credentials using public JSON endpoints.
# If you have approved API access, uncomment and fill in:
#
# REDDIT_CLIENT_ID=your_client_id
# REDDIT_CLIENT_SECRET=your_client_secret
# REDDIT_USER_AGENT=wsb-tracker/0.1.0 (by /u/your_username)

# =============================================================================
# Database Configuration
# =============================================================================
# Path to SQLite database file (supports ~ expansion)
WSB_DB_PATH=~/.wsb-tracker/wsb_tracker.db

# =============================================================================
# Scanning Settings
# =============================================================================
# Number of posts to scan per request (10-500)
WSB_SCAN_LIMIT=100

# Minimum post score to process (filters low-quality posts)
WSB_MIN_SCORE=10

# Sort method: hot, new, rising, top
WSB_SCAN_SORT=hot

# Subreddits to monitor (comma-separated)
WSB_SUBREDDITS=wallstreetbets

# =============================================================================
# Rate Limiting
# =============================================================================
# Delay between API requests in seconds (be respectful!)
WSB_REQUEST_DELAY=2.0

# =============================================================================
# Alert Configuration
# =============================================================================
# Enable/disable alert system
WSB_ENABLE_ALERTS=true

# Heat score threshold to trigger alerts (0-100)
WSB_ALERT_THRESHOLD=80.0

# =============================================================================
# Output Settings
# =============================================================================
# Default to JSON output (for scripting/pipelines)
WSB_JSON_OUTPUT=false

# Output directory for exports
WSB_OUTPUT_DIR=output
